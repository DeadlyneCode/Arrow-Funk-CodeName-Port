-function onPlayerHit(noteData) {
    switch(noteData) {
        case 0:
            playBFsAnim("hey", true);
        case 1:
            playBFsAnim("hey", true);
        case 2:
            playBFsAnim("hey", true);
        case 3:
            playBFsAnim("hey", true);
    }

}
function onDadHit(noteData) {
    switch(noteData) {
        case 0:
            playDadsAnim("hey", true);
        case 1:
            playDadsAnim("hey", true);
        case 2:
            playDadsAnim("hey", true);
        case 3:
            playDadsAnim("hey", true);
    }
}